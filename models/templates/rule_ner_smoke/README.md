# Rule NER smoke bundle

This is a runnable handoff template for SUTRA's deterministic NER adapter.
It contains no model weights and makes no performance claim. Copy the whole
directory into `models/incoming/`, give it a unique `id` and `version`, then
replace the template metrics with measured results before a real handoff.
