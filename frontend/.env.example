# Leave empty for the Vite development proxy. For a separately deployed API:
VITE_API_BASE_URL=http://localhost:8000
# Used only by `npm run dev` to proxy /api requests.
VITE_PROXY_TARGET=http://localhost:8000
